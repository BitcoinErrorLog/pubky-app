# Step-up approval for bridged sessions (Shop ↔ pubky.app)

**Status:** design proposal, 2026-09-05. Scope: the single signer moment a user arriving via the pubky.app session bridge hits at their first commerce action. No secret, cookie, or key material appears in this document.

## Context (verified)

- The bridge (`/session-bridge`, upstream ADR 0020) replies only to allowlisted origins with the public `sessionExport` (pubky + capabilities); the credential stays an HttpOnly homeserver cookie, one per pubky, `SameSite=None` (ADR 0020 lines 16–18, 40–49; `handler.ts:13–35`). The consumer side does not yet exist in this repo (no `pubky-session`/`SessionBridge`/`vibe-session` hits under `src/`); the prototype consumer that will be ported lives outside this repo (see sketch item 3).
- Shop's homeserver grant is `CAPABILITIES = '/pub/pubky.app/:rw,/pub/paykit/:rw,/priv/pubky.app/:rw'` (`src/core/services/homeserver/homeserver.ts:77`); the messaging grant `PAYKIT_MESSAGING_CAPABILITY` is byte-identical because a narrower approval replaces the one cookie and broke all `/pub/pubky.app` writes (`src/libs/commerce/messaging-contracts.ts:10–22`). A bridged (narrow, `/pub/pubky.app/:rw`-only) session therefore: browses and writes `/pub/pubky.app` fine; watchlist sync degrades to `needs_reauth` (`src/core/application/commerce/commerce.ts:845–848,862`); receipt publishing silently returns (`commerce.ts:983–985`); messaging resume fails with `SessionResumeScopeMissing` and falls through to the enable prompt (`src/core/services/paykit/paykit-messaging.ts:300–327`).
- Every transaction-service action needs a bearer session from `POST /v1/auth/sessions` (`src/core/services/marketplace/marketplace-session.ts:104–182`). The token comes from `HomeserverService.generateAuthTokenFlow()` = `pubkySdk.startAuthFlow('', AuthFlowKind.signin(), relay)` + `flow.awaitToken()` (`homeserver.ts:522–548`). The service verifies raw bytes via `AuthToken::verify`, enforces a server-clock window, and records `(pubky, timestamp)` in `auth_token_uses` for single-use (`marketplace-service/crates/service/src/auth.rs:84–105,155–181`). Capabilities are extracted and stored in `auth_sessions` (`auth.rs:101,186–195`). The Paykit claim uses its own exact-scope token (`src/core/services/marketplace/marketplace-paykit-claim.ts:14,69–92`).
- **SDK fact that constrains Option A:** `pubky.d.ts` exposes `AuthFlow.awaitApproval(): Promise<Session>` (line 188) and `AuthFlow.awaitToken(): Promise<AuthToken>` (line 198) as the two terminal calls on one flow, plus `AuthToken.toBytes()/verify/fromBytes` (lines 336–366). There is **no** `signinWithAuthToken` or any public API that converts an `AuthToken` into a homeserver `Session`/cookie. One approval yields exactly one of {Session, token bytes} — never both.

## Option A — one combined approval, token reused at two verifiers

```
Shop: startAuthFlow(CAPABILITIES, signin) → QR/deeplink
Ring: ONE approval (shows full Shop grant verbatim)
Shop: token = awaitToken()
  ├─ POST token bytes → homeserver session endpoint (manual; replaces cookie with wide grant)
  └─ POST token bytes → marketplace /v1/auth/sessions → bearer session
```

- **Feasibility is the blocker.** Getting the token _bytes before_ homeserver sign-in is only possible via `awaitToken()`, but the SDK then offers no sign-in-with-token call. The homeserver cookie could only be set by re-implementing the protocol-internal session POST that `awaitApproval()` performs inside WASM (not visible in `pubky.d.ts`; exact endpoint/semantics unverifiable from the repos read). **Unverified:** whether the homeserver enforces AuthToken single-use — if it does, the same bytes cannot be accepted by both verifiers and Option A is impossible regardless.
- **Replay between verifiers:** the token is a bearer credential the service accepts only inside its 120 s window (`AUTH_TOKEN_WINDOW_SECONDS` default, `marketplace-service/crates/service/src/config.rs:93`; enforced at `auth.rs:96–98`) — tighter than the pubky-common library ceiling of 180 s (`auth.rs:44–48`), so 120 s is the effective race window. Whoever holds the bytes can, inside those 120 s, (a) race Shop to `/v1/auth/sessions` — single-use means first poster wins, and (b) if the homeserver accepts it, mint a **wide-grant cookie session in an attacker's browser**. In Options B/C the service token is empty-caps, so stealing it yields only a marketplace session, never homeserver access. This is a strictly worse blast radius for a value that transits JS memory.
- **What the service learns:** the full grant string — including `/priv/pubky.app/:rw` — is persisted in `auth_sessions.capabilities` (`auth.rs:186–195`) and returned to the client (`marketplace-session.ts:32–37`). Scope disclosure to the operator with no compensating benefit.
- **pubky.app tab after cookie replacement:** its stored `sessionExport` still lists narrow caps; `restoreSession` revalidates via the cookie (ADR 0020 line 16; `pubky.d.ts:831`). Whether it re-checks the export's capabilities against the live session is **not documented** in the SDK — uncertain; worst cases are stale reported caps (harmless: the wide grant is a superset of `/pub/pubky.app/:rw`) or a restore failure (needs an upstream fix). Must be tested empirically before shipping any option that widens the cookie.
- **Failure modes:** Ring declined → `awaitToken()` rejects (`pubky.d.ts:195–197`); nothing changed. Relay down → 120 s timeout frees the flow; dead QR by design (`marketplace-session.ts:19–27`). Service down _after_ homeserver sign-in → user holds a wide cookie but no service session; the token is re-POSTable only until the service records it — a crash between the single-use INSERT (`auth.rs:157–165`) and the response burns it and forces a **second** approval, defeating the design.

## Option B — status quo: wide homeserver re-approval + empty-caps service token

```
Bridged session (narrow) → first /priv or messaging use:
  Approval 1: startAuthFlow(CAPABILITIES) → awaitApproval() → wide cookie
First commerce action:
  Approval 2: generateAuthTokenFlow('') → awaitToken() → POST /v1/auth/sessions
```

Two Ring approvals for full functionality. Each token has one verifier, least-privilege holds (service token carries no homeserver scope), and no SDK gaps — everything uses public APIs. Costs: the user approves twice; approval 1 replaces the cookie (superset grant, so pubky.app keeps working — `messaging-contracts.ts:10–17`), and a later pubky.app sign-in reverts the cookie to narrow, requiring the degradation paths below. Failure modes are independent per approval, so partial completion always leaves a coherent state.

## Option C — defer the homeserver widening (recommended)

```
Bridged session (narrow) → first commerce action (checkout/offer/bid/…):
  ONE approval: generateAuthTokenFlow('') → awaitToken() → /v1/auth/sessions  ✅ done
Lazily, only when the user touches a scope-gated feature:
  watchlist sync → existing needs_reauth affordance → generateAuthUrl() wide re-approval
  messaging      → existing enable flow (SessionResumeScopeMissing path)
  receipts       → surfaced "paused" status instead of silent no-op → same re-approval
```

A buyer's first checkout costs **exactly one approval on every payment method** (Stripe, PayPal, BTC — see the per-method table below) with zero token reuse; seller BTC onboarding adds one one-time approval (the Paykit claim). The service session never required any homeserver capability (`homeserver.ts:522` default `''`; `auth.rs` records whatever caps arrive, empty included). The "service accepts a homeserver-cookie-backed proof" variant is infeasible: the challenge flow was removed precisely because the browser client never holds the secret key (`marketplace-service/README.md:571–579`), and the HttpOnly cookie is invisible to any third-party verifier — the AuthToken remains the only viable service credential, but nothing in the code forces the homeserver re-approval to share that moment. Graceful-degradation paths that must exist (and mostly do): watchlist `needs_reauth` UI (`commerce.ts:845–848`; `MarketplaceWatchlist.tsx:110`), messaging enable prompt (`paykit-messaging.ts:319–327`), and receipts must become visible-instead-of-silent (currently `commerce.ts:985` returns without a trace). Capability gating always reads the homeserver's own session facts (`homeserver.ts:121–141`), so a later pubky.app sign-in that reverts the cookie degrades Shop cleanly and the re-approval affordances reappear.

## Self-attack table

| Attack                                                                                | Precondition                                                        | Option A                                                                             | Option B                                    | Option C                                                                                                                                                                                                                  | Mitigation                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Steal marketplace bearer from `localStorage` (`marketplace-session.ts:29–30,236–243`) | XSS on shop.pubky.app                                               | Bearer usable until service TTL                                                      | Same                                        | Same                                                                                                                                                                                                                      | Short service TTL; 401 clears + reconnect affordance (`marketplace-session.ts:80–82`); strict CSP                                                                                                                                                                                                                                                             |
| Ride the bridged homeserver session (writes via cookie)                               | XSS on shop.pubky.app                                               | Wide grant incl. `/priv` after step-up                                               | Wide after re-approval 1                    | Narrow until a gated feature is used                                                                                                                                                                                      | Defer widening (C); gate on `canCurrentSessionWrite` (`homeserver.ts:136`)                                                                                                                                                                                                                                                                                    |
| Steal AuthToken bytes in flight                                                       | XSS / malicious extension during flow                               | Token mints **wide homeserver session** + races service single-use                   | Token yields marketplace session only       | Same as B                                                                                                                                                                                                                 | Never log bytes; POST immediately; keep token in one async scope                                                                                                                                                                                                                                                                                              |
| Replay AuthToken at service                                                           | Obtained bytes                                                      | Rejected: single-use `(pubky,timestamp)` (`auth.rs:155–181`)                         | Same                                        | Same                                                                                                                                                                                                                      | Service 120 s window (`config.rs:93`; `auth.rs:96–98`); library 180 s ceiling (`auth.rs:48`)                                                                                                                                                                                                                                                                  |
| Replay AuthToken at homeserver                                                        | Obtained bytes                                                      | **New wide cookie session for attacker if homeserver lacks single-use (unverified)** | Empty-caps token: no homeserver value       | Same as B                                                                                                                                                                                                                 | Confirm homeserver single-use upstream before ever considering A                                                                                                                                                                                                                                                                                              |
| QR/phish swap                                                                         | Attacker swaps displayed QR                                         | One approval shows full grant verbatim — over-broad consent                          | Two prompts; empty-caps prompt is low-value | Empty-caps first prompt is least persuasive                                                                                                                                                                               | Ring shows a scoped caps string verbatim (`marketplace-paykit-claim.ts:11–13`); **unverified UX assumption:** what Ring displays for an empty-capabilities request (`caps=''`) — if it does not visibly distinguish empty from wide, the "low-value prompt" reasoning above is unproven (verification step below); render requested caps in-app beside the QR |
| Bridge origin spoof                                                                   | Non-allowlisted origin posts message                                | Ignored, no reply (`handler.ts:20–31`; ADR 0020 lines 34, 42–49)                     | Same                                        | Same                                                                                                                                                                                                                      | Exact-origin allowlist + CSP `frame-ancestors` from one env list                                                                                                                                                                                                                                                                                              |
| Silent downgrade after pubky.app re-sign-in                                           | User signs into pubky.app again                                     | Cookie reverts narrow; all gated features must degrade                               | Same                                        | Same (by design)                                                                                                                                                                                                          | `needs_reauth` surfaces, enable prompt, visible receipts status                                                                                                                                                                                                                                                                                               |
| Allowlisted vibe writes via the shared cookie (first-party code, **not** XSS)         | A legitimately allowlisted vibe turns hostile or is pushed bad code | Wide grant incl. `/priv` after step-up                                               | Wide after re-approval 1                    | Narrow until a gated feature is used — **same exposure under all options once the user has widened**: any allowlisted vibe can write `/pub/pubky.app/**` (and `/priv`, `/pub/paykit` once granted) with the shared cookie | Vetting + exact-origin allowlist before any DNS record (ADR 0020 line 20, 47–51); per-vibe scoped sessions (ADR 0020 alternative 3, lines 105–118) as the future fix                                                                                                                                                                                          |

## Recommendation

**Option C.** It meets the "at most ONE Ring approval at first commerce action" goal using only public, shipped SDK APIs; keeps the empty-caps service token worthless against the homeserver; avoids persisting the `/priv` grant string in the service database; and reuses degradation paths that already exist. Option B is the safe fallback if product insists on widening eagerly. **Option A is rejected**: the SDK exposes no AuthToken→Session conversion (checked `pubky.d.ts`: only `awaitApproval`/`awaitToken`/`restoreSession`), it would depend on unverified homeserver single-use behaviour, and it turns the in-flight token into a wide-grant bearer credential.

Approvals per payment method and feature under Option C:

| Flow                 | Ring approvals                                                               | What the approval is                                                                                                                                                                                                      |
| -------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Stripe checkout      | 1                                                                            | Empty-caps service token (`generateAuthTokenFlow('')` → `/v1/auth/sessions`)                                                                                                                                              |
| PayPal checkout      | 1                                                                            | Same service token                                                                                                                                                                                                        |
| BTC checkout (buyer) | 1                                                                            | Same service token — `payment.register_locks` is a service-side command under the marketplace bearer session and the Locks correlation (`beginMarketplaceLocksPayment`, `commerce.ts:556`) needs no buyer signer approval |
| BTC seller setup     | 2                                                                            | Service token + one-time Paykit claim (`PAYKIT_CLAIM_CAPABILITIES`, `marketplace-paykit-claim.ts:14`; flow at `:69`, posted to **paykit-server**, not the marketplace service)                                            |
| Watchlist sync       | +1, deferred                                                                 | Wide `generateAuthUrl(CAPABILITIES)` re-approval, only when sync is first used                                                                                                                                            |
| Messaging            | 0 if the cookie already carries `/pub/paykit`; +1 for narrow/legacy sessions | `SessionResumeScopeMissing` → enable flow (`paykit-messaging.ts:300–327`)                                                                                                                                                 |
| Receipts             | 0 additional                                                                 | Covered by the same widened cookie once any wide re-approval lands                                                                                                                                                        |

## Implementation sketch (Option C)

**Status:** implemented on branch `marketplace/stepup-ux`, 2026-09-05. Sketch items 1–5 landed; sketch item 3 (vibe-session consumer port) was already in the base branch (ADR 0029).

**mp-ux:**

1. `src/core/application/commerce/commerce.ts:982–985` — replace the silent `canCurrentSessionWrite` return with a recorded/surfaced receipts status (mirror the watchlist `needs_reauth` pattern at `commerce.ts:845–877`; add the status type in `src/core/stores/commerce/commerce.types.ts:17–23`).
2. `src/components/templates/Marketplace/MarketplaceWatchlist.tsx:110` — wire the existing `needs_reauth` affordance to a hook that calls `HomeserverService.generateAuthUrl()` (default `CAPABILITIES`, `homeserver.ts:487–503`) and, on approval, updates the auth store session.
3. Port the existing vibe-session consumer (prototype commit `30e950d2`, ADR 0024) onto this branch: `src/libs/vibe-session/*` — bridge, fragment, auto-restore, config, expired, types, should-restore — plus their tests, configured by `NEXT_PUBLIC_VIBE_SESSION_BRIDGE_ORIGIN` (consumer mode active only when set). Restore goes through `AuthApplication.restorePersistedSession` and `AuthController` per ADR 0024 (Controller owns store writes), **not** `HomeserverService.restoreSession` directly. On a bridged restore no re-approval is auto-triggered — the restored session keeps its narrow grant until a gated feature asks for more. The port must preserve Shop's post-restore staging guard `assertUserHomeserverAllowed` (`homeserver.ts:182–207`), which only runs when `PUBKY_RUNTIME_ENV=staging` (`src/config/network.ts:27,37`).
4. Messaging: no change — cookie-resume and the `SessionResumeScopeMissing` → enable flow already handle narrow sessions (`paykit-messaging.ts:264–327`); the enable grant stays `PAYKIT_MESSAGING_CAPABILITY` in lockstep with `CAPABILITIES`.
5. First-commerce-action path: unchanged — `CommerceApplication` already funnels to `MarketplaceSessionService.beginSessionFlow()` (`commerce.ts:372`), the single approval.

**marketplace-service:** no changes for C. (Add one test only, below.)

**Tests to add:**

- mp-ux: bridged narrow session → first checkout triggers exactly one `beginSessionFlow` and zero `generateAuthUrl` calls (mock SDK; extend `commerce.watchlist.test.ts` style).
- mp-ux: receipt publish under narrow grant records the new visible status instead of silently returning (covers `commerce.ts:985`).
- mp-ux: watchlist reauth CTA invokes `generateAuthUrl` with the exact `CAPABILITIES` string; after approval, watchlist + receipts + messaging resume all flip to capable.
- mp-ux: messaging resume on a narrow cookie yields the enable prompt (extend `paykit-messaging.test.ts` mock at line 156 with a bridged-session case).
- mp-ux: manual test (then VRT-adjacent note): pubky.app tab's `restoreSession` after Shop widens the cookie — behaviour currently undocumented in the SDK; verify before launch.
- Manual, blocks launch: test what Pubky Ring displays for an empty-capabilities pubkyauth request (`caps=''`); if it does not visibly distinguish empty from wide, file a Ring issue before launch (the QR/phish-swap row's "low-value empty-caps prompt" reasoning depends on this).
- marketplace-service: `create_session` accepts an empty-capabilities token; posting identical bytes twice returns 401 on the second call (integration-level replay test — current `auth.rs:269–374` tests cover verification, not the single-use INSERT path).

## Verification that differed from the brief

1. **`signinWithAuthToken` does not exist.** `pubky.d.ts` has no AuthToken→Session API (only `AuthFlow.awaitApproval` line 188, `awaitToken` line 198, `Pubky.restoreSession` line 831, `Signer.signin` line 1294). Option A's "same bytes sign in to the homeserver" requires re-implementing protocol internals, not an SDK call.
2. **No bridge consumer exists in mp-ux yet** — grep for `pubky-session` / `SessionBridge` / `vibe-session` under `src/` returns nothing; the upstream provider side and the prototype consumer (`pubky-app-wt-marketplace` commit `30e950d2`, ADR 0024) both live outside this repo.
3. **Line ranges were slightly off** (claims themselves correct): `generateAuthTokenFlow` spans `homeserver.ts:522–548`, not 506–545; the receipt no-op guards are `commerce.ts:983–985` (function starts 982), not 981–985; `PAYKIT_CLAIM_CAPABILITIES` is at `marketplace-paykit-claim.ts:14`, within the cited 8–15.
4. **Unverifiable claims flagged, not asserted:** homeserver-side AuthToken single-use; whether `restoreSession` re-checks export capabilities against the live session. Both need empirical or upstream confirmation before any widening ships.

## Review fixes applied 2026-09-05

Independent adversarial review: FIX-FIRST (0 P0, 2 P1, 4 P2). Applied:

1. **P1-1 (BTC approvals omitted):** added the per-payment-method approval table to the Recommendation; replaced the unqualified "exactly one approval" with the qualified statement (true for a buyer's first checkout on every method; seller BTC onboarding adds one one-time approval — the Paykit claim to paykit-server, `marketplace-paykit-claim.ts:14,69`).
2. **P1-2 (sketch item 3 conflicted with the prototype):** replaced it with a port of the existing `src/libs/vibe-session/*` consumer (prototype commit `30e950d2`, ADR 0024) via `AuthApplication.restorePersistedSession`/`AuthController`, configured by `NEXT_PUBLIC_VIBE_SESSION_BRIDGE_ORIGIN`; no auto re-approval on bridged restore; staging guard `assertUserHomeserverAllowed` (`homeserver.ts:182–207`) preserved.
3. **P2-1:** added the self-attack row for a legitimately allowlisted vibe writing `/pub/pubky.app/**` via the shared cookie — same exposure under all options; mitigation: vetting + exact-origin allowlist before any DNS record, per-vibe scoped sessions (ADR 0020 alternative 3) later.
4. **P2-2:** flagged Ring's rendering of an empty-capabilities (`caps=''`) request as an unverified UX assumption in the threat table and added a launch-blocking verification step; nothing is asserted about what Ring shows.
5. **P2-3:** corrected the token window — two windows exist (pubky-common library 180 s, `auth.rs:44–48`; service `AUTH_TOKEN_WINDOW_SECONDS` default 120 s, `config.rs:93`); effective race window is 120 s. The "~3 min" statement and replay-race reasoning were fixed.
6. **P2-4:** corrected line citations to HEAD `64cb7aee`: `generateAuthTokenFlow` `homeserver.ts:522–548`; watchlist `needs_reauth` `commerce.ts:845–848,862` (was 847,887,926); every other citation re-checked with `sed -n` and tightened where off by a line or two (`auth.rs`, `paykit-messaging.ts`, `commerce.types.ts`).
