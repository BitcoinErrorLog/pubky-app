Required launch-e2e gate serves a committed Nexus fixture instead of live 7108. Checkout hydrates stub listing 2b81df4f from Dexie (no WASM homeserver GET). Signed-in seller board with grant is the required Chromium canary so listSellerListings runs. The page origin is 127.0.0.1, so the gate forwards staging-api.pubky.app through Node after the page calls fetch; it does not read live nexusd. Catalog axe keeps color-contrast as a hard gate.
