Auction bids now use the authenticated bidder’s authoritative minimum when a private proxy maximum requires a higher bid.
Auction bids now show the visible-price minimum and explain typed service refusals without closing the bid dialog.
