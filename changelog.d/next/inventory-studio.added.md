Sellers can list and set available stock on an Inventory board that uses a marketplace-service grant separate from the purchase session.

Sellers can import canonical CSV or JSON listings in the browser, dry-run the plan, publish with homeserver PUT then `listing.sync_many` chunks of at most 100, and resume unfinished rows after a mixed 207.
