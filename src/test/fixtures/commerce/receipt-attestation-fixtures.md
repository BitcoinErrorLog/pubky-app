These live artifacts were produced by the real marketplace service at commit
`fcd38b2` from `contracts/samples/`. They are copied byte-for-byte from the
responses returned by `GET /v1/receipts/{id}/attestation`.
